<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head'); %>
  </head>
  <body class="container">
    <header><%- include('../partials/header'); %></header>

    <script>
      const handleSubmit = (e) => {
        let data = { data: [] };

        let tmpObj = {};

        const toJson = (key, val, buffer) => {
          const obj = { [key]: val };
          console.log(obj);
          debugger;
          buffer.push(obj);
          return;
        };

        for (let i = 0; i < e.length; i++) {
          const id = e[i].id.toString();
          const value = e[i].value.toString();

          const obj = { [id]: value };
          tmpObj = Object.assign(obj, tmpObj);

          // toJson(id, value, tmpObj);
        }
        console.log(tmpObj);

        debugger;
      };
    </script>

    <main class="d-flex align-items-center justify-content-center">
      <!-- <div class="row justify-content-center"> -->
      <div class="row">
        <div class="col-sm-8">
          <div class="jumbotron">
            <h1>Add Books</h1>
          </div>
          <form
            id="addBook"
            onsubmit="return handleSubmit(this.inputs)"
            class="form-row"
            method="get"
            action="/add"
          >
            <% keys.forEach(i => { %>
            <div class="form-group col-md-6">
              <label for="<%= i %>"><%= i.toUpperCase() %></label>
              <input
                type="text"
                name="inputs"
                class="form-control"
                id="<%= i %>"
                required
              />
            </div>
            <% }); %>
            <button type="submit" id="submitButton" class="btn btn-primary">
              Submit
            </button>
          </form>
        </div>
      </div>
    </main>

    <footer><%- include('../partials/footer'); %></footer>
  </body>
</html>
